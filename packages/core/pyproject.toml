[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dgk-core"
version = "0.1.0"
description = "Core library for dev-graph-kit"
requires-python = ">=3.11"
dependencies = ["tomli-w>=1.0", "pydantic>=2.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/dgk_core"]
